<h2>Record Details</h2>
<br/>
<div class="row">
  <div class="col-md-6">
    <img class="float-left mr-3" src="{{ record.model_image }}" width="100" height="100"/>
    <h5>{{ record.title }}</h5>
    <p>{{ record.description }}</p>
  </div>
  <div class="col-md-12 mt-5">
    <a class="btn btn-primary" href="/edit/{{ record.id }}" class="btn btn-link">Edit</a>
  </div>
</div>
