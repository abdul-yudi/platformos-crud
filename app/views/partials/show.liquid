<h2>Record Details</h2>
<br/>
<div class="row">
  <div class="col-md-6">
    <img class="float-left mr-3" src="{{ record.featured.url }}" width="50" height="50"/>
    <h6>{{ record.title }}</h6>
    <p>{{ record.description }}</p>
  </div>
  <div class="col-md-12 mt-5">
    <a class="btn btn-primary" href="/edit/{{ record.id }}" class="btn btn-link">Edit</a>
  </div>
</div>
